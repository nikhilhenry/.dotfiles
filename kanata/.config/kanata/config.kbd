(defsrc
  caps
  enter
  rshift
)

(defalias
    esccrtl (tap-hold 100 100 esc lctl)
)

(defalias
    entersway (tap-hold 100 100 enter met)
)

(defalias
    bspcrshift (tap-hold 100 100 bspc rshift)
)

(deflayer base
    @esccrtl
    @entersway
    @bspcrshift
)
